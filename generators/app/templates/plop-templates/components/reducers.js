// Start by importing some actions and defining the initial state.
import { DEFAULT_ACTION } from './actions';

const initialState = {};

function {{camelCase componentName }}Reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default {{ camelCase componentName }}Reducer;
